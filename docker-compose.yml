version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aptitude-frontend
    expose:
      - "80"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`121.148.209.10`)"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
    networks:
      - aptitude-net
      - coolify

networks:
  aptitude-net:
    external: true
    name: aptitude-net
  coolify:
    external: true
    name: coolify