version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aptitude-frontend
    networks:
      - aptitude-net
      - coolify

networks:
  aptitude-net:
    external: true
    name: aptitude-net
  coolify:
    external: true
    name: coolify